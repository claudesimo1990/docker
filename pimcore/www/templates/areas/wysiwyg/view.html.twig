{% set content = pimcore_wysiwyg('content') %}

{# add autogenerated meta description #}
{% if document.getDescription() is empty %}
    {% do pimcore_head_meta().setDescription(content.getData(), 160) %}
{% endif %}

{% include 'includes/settings.html.twig' %}

<section class="{% if pimcore_checkbox('background').isChecked() %} about section-bg {% else%} area-wysiwyg {% endif %}">
    {% pimcoreglossary %}
        {{ content|raw }}
    {% endpimcoreglossary %}
</section>
